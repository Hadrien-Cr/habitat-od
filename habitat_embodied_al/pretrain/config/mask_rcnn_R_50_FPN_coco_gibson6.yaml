# Training overlay for pretrain/, Gibson-Semantic (COCO6) variant -- see
# mask_rcnn_R_50_FPN_coco.yaml (HSSD) for the shared shape. MODEL.ROI_HEADS.NUM_CLASSES is set
# at runtime by pretrain.py from the registered dataset's thing_classes (6, per
# ds_gibson_coco6.yaml's filter_classes).
_BASE_: "Base-RCNN-FPN.yaml"
MODEL:
  WEIGHTS: "detectron2://COCO-InstanceSegmentation/mask_rcnn_R_50_FPN_3x/137849600/model_final_f10217.pkl"
  MASK_ON: False
  RESNETS:
    DEPTH: 50
